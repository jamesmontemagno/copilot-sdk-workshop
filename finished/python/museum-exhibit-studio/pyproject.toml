[project]
name = "museum-exhibit-studio"
version = "1.0.0"
description = "A grounded museum exhibit generator using the GitHub Copilot SDK."
requires-python = ">=3.11"
dependencies = [
  "github-copilot-sdk==1.0.11",
]

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
